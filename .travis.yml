language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t mistydev/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true mistydev/docker-react yarn test
